/** @type {import('next').NextConfig} */
const nextConfig = {
    output: "export",
    basePath: "/geesisila-website",
    images: {
        unoptimized: true,
      },
};

module.exports = nextConfig;
